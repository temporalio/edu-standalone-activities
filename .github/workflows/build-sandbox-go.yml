name: Build Go sandbox image

# Builds the Instruqt sandbox image used by the Go standalone-activities
# track and pushes it to GitHub Container Registry.
#
# Triggers on changes to the Dockerfile or to the pre-cloned course repo, since
# course-repo is COPY'd into the image at build time. Also runnable on demand.
# Includes add-go-track so the image rebuilds while the track is still in
# development, before it is merged to main.

on:
  push:
    branches:
      - main
      - add-go-track
    paths:
      - 'go/sandbox/**'
      - 'go/course-repo/**'
      - '.github/workflows/build-sandbox-go.yml'
  pull_request:
    paths:
      - 'go/sandbox/**'
      - 'go/course-repo/**'
      - '.github/workflows/build-sandbox-go.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

env:
  IMAGE: ghcr.io/temporalio/edu-standalone-activities-sandbox

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Set up Buildx
        uses: docker/setup-buildx-action@8d2750c68a42422c14e847fe6c8ac0403b4cbd6f # v3.12.0

      - name: Log in to GHCR
        # Only log in for events that should publish. PRs build but don't push.
        if: github.event_name != 'pull_request'
        uses: docker/login-action@c94ce9fb468520275223c153574b00df6fe4bcc9 # v3.7.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build (and push, if not a PR)
        uses: docker/build-push-action@10e90e3645eae34f1e60eeb005ba3a3d33f178e8 # v6.19.2
        with:
          # Context is go/ so the Dockerfile's `COPY course-repo/` resolves.
          context: go
          file: go/sandbox/Dockerfile
          # Instruqt sandboxes run linux/amd64. Pin explicitly so this matches
          # regardless of who triggers the build or what host arch they're on.
          platforms: linux/amd64
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.IMAGE }}:go-latest
            ${{ env.IMAGE }}:go-${{ github.sha }}
          cache-from: type=gha,scope=go-sandbox
          cache-to: type=gha,scope=go-sandbox,mode=max
