[project]
name = "webhooks"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "temporalio>=1.27",
  "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/webhooks"]
