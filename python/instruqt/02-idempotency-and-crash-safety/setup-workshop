#!/usr/bin/env bash
set -euo pipefail

# Wipe prior module's source and seed Module 02's exercise.
rm -rf /root/workshop/src
cp -r /opt/workshop/exercise/02-idempotency-and-crash-safety/. /root/workshop/

# Clean echo + worker.
pkill -f "python -m webhooks.worker" || true
curl -fsS -X POST http://localhost:9000/_reset > /dev/null || true
