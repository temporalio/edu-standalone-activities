#!/usr/bin/env bash
set -euo pipefail
DIR=/root/workshop/exercises/04-concurrency-and-rate-limits/exercise
cp -rf /opt/workshop/solution/04-concurrency-and-rate-limits/. "$DIR/"
# Also clear the receiver rate limit so it doesn't leak into Module 05 if Instruqt
# skips past us.
curl -fsS -X POST 'http://localhost:9000/_rate_limit?limit=0' > /dev/null || true
