# Applies only to builds that use the repository root as context (the Java sandbox image,
# which needs both java/course-repo and shared/webhook-receiver). Other language tracks
# build with their own <lang>/ context and are unaffected by this file.
.git
**/.gradle
**/build
**/node_modules
**/.DS_Store
docs
course-repo
python
